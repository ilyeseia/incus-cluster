cluster:
  enabled: true
  server_name: "{{ inventory_hostname }}"
  server_address: "{{ tailscale_ip }}:{{ incus_https_port }}"
  cluster_token: "{{ incus_join_token }}"
  member_config:
    - entity: storage-pool
      name: "{{ incus_storage_pool }}"
      key: source
      value: ""
